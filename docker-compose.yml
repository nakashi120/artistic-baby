version: "3"

services:
  rest_api:
    build:
      context: .
      dockerfile: DockerfilePython
    ports:
      - "8000:8000"
    volumes:
      - ./code/django_rest_api:/code
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
